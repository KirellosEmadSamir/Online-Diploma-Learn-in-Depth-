#include <stdio.h>
#include <stdlib.h>

int main()
{
    float x,y;
    printf("Enter Two numbers to multiply");
    scanf("%f %f",&x,&y);
    printf("Mult. Of %f*%f Equal %f",x,y,x*y);
    return 0;
}
